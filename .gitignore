node_modules/
dist/
connector.config.json
# Local integration-test rigs -- they bake in account-specific things
# (inference profile ARNs, personal checkouts) that don't belong in the repo.
examples/
